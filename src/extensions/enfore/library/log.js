/**
 * Log library
 *
 * Provides more transparent way to log to console.
 */

/**
 * Logs message to console after a given prefix.
 * @param  {id} message the object to print
 */
export default function logMessage (message) {
  log('SKETCH PLUGIN: ' + message)
}
