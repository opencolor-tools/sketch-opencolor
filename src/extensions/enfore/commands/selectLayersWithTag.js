/**
 * Select layers with tag
 *
 * Selects all layers on the current page that are tagged with the specified tag.
 */

import Context from '../library/context'

export default (context) => {
  Context(context)
}
