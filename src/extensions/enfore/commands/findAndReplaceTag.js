/**
 * Find and replace tag
 *
 * Finds all layers tagged with a tag and replaces that tag with the provided replacement.
 */

import Context from '../library/context'

export default (context) => {
  Context(context)
}
