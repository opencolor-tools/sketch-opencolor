/**
 * Toggle mute indicators
 *
 * Shows or hides mutes indicators to highlight muted layers.
 */

import Context from '../library/context'

export default (context) => {
  Context(context)

  // TODO how to add markers to canvas?
}
