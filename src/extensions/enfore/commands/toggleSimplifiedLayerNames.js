/**
 * Toggle simplified layer names
 *
 * Toggles between full tags being used for names and abbreviated tags for clarity.
 */

import Context from '../library/context'

export default (context) => {
  Context(context)
}
