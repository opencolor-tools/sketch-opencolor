/**
 * Export mapping
 *
 * Exports tags assigned to layers as a color mapping in standardized JSON format.
 */

import Context from '../library/context'

export default (context) => {
  Context(context)
}
