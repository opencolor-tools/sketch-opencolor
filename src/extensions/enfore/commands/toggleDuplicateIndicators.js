/**
 * Toggle duplicate indicators
 *
 * Shows or hides duplicate indicators to highlight duplicate tag assignments on layers.
 */

import Context from '../library/context'

export default (context) => {
  Context(context)

  // TODO how to add markers to canvas?
}
