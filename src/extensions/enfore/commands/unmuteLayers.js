/**
 * Unmute layers
 *
 * Ensures that selected layers are again considered in mapping export if previously muted.
 */

import Context from '../library/context'

export default (context) => {
  Context(context)
}
